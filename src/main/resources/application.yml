spring:
  datasource:
    # sslmode 파라미터를 안전한 구문 형식으로 지정합니다.
    url: jdbc:postgresql://aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.bgincodjshyqtvecamzz
    password: Ineedf126soi
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 2
      minimum-idle: 1
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        # PostgreSQL SSL 파싱 지원
        temp:
          use_jdbc_metadata_defaults: false

  test:
    database:
      replace: none